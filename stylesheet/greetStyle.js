import { StyleSheet } from "react-native";

const greetStyles = StyleSheet.create({
  greetView: {
    padding: 20,
  },
  greetText: {
    fontSize: 24,
    textAlign: "center",
    fontWeight: "600",
  },
});

export default styles;
